{% extends 'base.html.twig' %}

{% block title %}Championnats{% endblock %}

{% block body %}
    <h1>Championnats</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ championnat.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ championnat.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_championnats_index') }}">back to list</a>

    <a href="{{ path('app_championnats_edit', {'id': championnat.id}) }}">edit</a>

    {{ include('championnats/_delete_form.html.twig') }}
{% endblock %}
